import sys

print('0. pandas', flush=True)
import pandas
print('1. numpy', flush=True)
import numpy
print('2. faiss', flush=True)
import faiss
print('3. sklearn', flush=True)
import sklearn
print('4. sklearn.feature_extraction.text', flush=True)
import sklearn.feature_extraction.text
print('5. sklearn.preprocessing', flush=True)
import sklearn.preprocessing
print('6. src.config', flush=True)
sys.path.append('d:/LICENTA/LICENTA_SERVERSIDE')
import src.config
print('Done!', flush=True)
